# Learning inbox

Sanitized candidates for the atlas. **Not** written into `data/graph.json` yet.

## Files

- `03-providers.md` — source notes. **Merged into `data/graph.json`** (providers, models,
  Langfuse, patterns). Keep this file as the review trail; further edits go via the editor.
- `04-voice-multimodal-notes.md` — from personal Voice-Multimodal notes. **Merged into
  `data/graph.json`** (audio foundations, codecs, speech LMs, TTS models, eval).

## Rules

- Generic voice-agent learning only — no company, client, product, or ops detail.
- Point-in-time numbers (TTFT, prices) are examples, not SLAs.

## How to use

1. In the pending candidate file, mark each item `[ ] keep` / `edit` / `drop`.
2. Tell me the keepers (or edit inline).
3. I add approved items to the atlas via `graph.json` (nodes/edges/notes).
