{
  "name": "voice-agent-atlas",
  "version": "1.0.0",
  "private": true,
  "description": "Local viewer + editor for the voice agent knowledge graph",
  "scripts": {
    "start": "node server.js",
    "build:css": "tailwindcss -i css/tailwind.src.css -o css/styles.css --optimize",
    "watch:css": "tailwindcss -i css/tailwind.src.css -o css/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
